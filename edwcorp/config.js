/* ============================================================
   EdwCorp — Configuración del sitio

   Este es el ÚNICO archivo que hay que tocar para conectar el
   formulario y el diagnóstico con tu base de datos.

   Pega aquí la URL de tu aplicación web de Apps Script
   (la que termina en /exec) y guarda. Eso es todo.
   ============================================================ */

var EDW_ENDPOINT = "";

/* Correo al que se abre el respaldo si el endpoint está vacío. */
var EDW_CORREO = "admin@edwcorp.org";
